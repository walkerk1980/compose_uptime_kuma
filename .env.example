# Environment Variables for ClipCascade with nginx SSL Proxy
# Copy this file to .env and update the values according to your setup

# Hostname and Domain Configuration
# These will be used to construct the SSL certificate filenames and server name
# Certificate files expected: ${HOSTNAME}.${DOMAIN}.crt and ${HOSTNAME}.${DOMAIN}.key
HOSTNAME=your-hostname
DOMAIN=example.com

# SSL Certificate Directories (handled by Docker volume mounts)
# The docker-compose.yml mounts these directories:
# ${SSL_CERTS_DIR} -> /etc/nginx/certs (certificate files)
# ${SSL_KEY_DIR} -> /etc/nginx/private (private key files)
SSL_CERTS_DIR=/etc/ssl/certs
SSL_KEY_DIR=/etc/ssl/private

